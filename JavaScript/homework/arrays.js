const numbers = [1, 2, 4, 9, -9, 99, 6, 4, 7, 5, 33, 5];
const results = [];

console.group('Using numbers in the array<numbers> form new numbers, and add them to array<results>');

// example 
console.group('Form number 13, and the to the end of the array');
results[0] = numbers[2] + numbers[3];
console.groupEnd();
// ----------------------------------------------------------------------
console.group('Form number 15 and add it to the end of the array<results>');
// code ...
console.groupEnd();

// ----------------------------------------------------------------------

console.group('Form number -15 and add it to the end of the array<results>');
// code ...
console.groupEnd();

// ----------------------------------------------------------------------

console.group('Form number -26 and add it to the front of the array<results>');
// code ...
console.groupEnd();

// ----------------------------------------------------------------------

console.group('Form number 126 and add it to the front of the array<results>');
// code ...
console.groupEnd();

// ----------------------------------------------------------------------

console.group('Form number 111 and add it to the middle of the array<results>');
// code ...
console.groupEnd();

// ----------------------------------------------------------------------

console.group('Form number 69 and add it to the middle of the array<results>');
// code ...
console.groupEnd();






console.groupEnd();

